## 2026-07-18

Done:
- Added apps/firm-gateway scaffold with MCP-style HTTP tool dispatch.
- Implemented get_quote with deterministic quote math and PRICING_MODE switch: TIERS default, QUOTED_AMOUNT optional.
- Implemented execute inserting paid jobs into firm_jobs for the F3 worker.
- Implemented get_status and get_result reads from firm_jobs.
- Added express_run placeholder that returns TODO_UNVERIFIED_EXPRESS_VENDOR_POOL instead of inventing an Express vendor.
- Added tools/vendor-index hook that requires MARKETPLACE_SCAN_JSON and refuses to invent live marketplace data.
- TypeScript build passed before cleanup via package-local tsc.

Blocked:
- Seller-side x402 charging adapter is not copied from Treasury because Treasury is absent from this repo and untouchable without Poulav's explicit go.
- Live marketplace scanner source is absent; vendor-index generation requires a real scan JSON input.

Next:
- Integrate the real Treasury-style seller-side charging pattern when Poulav provides/approves the source.
- Add persistent quote storage; current gateway quote storage is in-memory scaffold.
- Replace Express placeholder after vendor reliability testing locks the Express job type.

Questions for humans:
- Where is the existing marketplace scanner source that tools/vendor-index should reuse?

## 2026-07-18

Done:
- Added root pnpm workspace metadata for apps/packages/tools/tests.
- Built apps/firm-gateway successfully with TypeScript.
- Gateway insert/read contract now aligns with apps/firm/GATEWAY_HANDOFF.md.

Blocked:
- Gateway quote storage is still in-memory; needs Postgres quote table before multi-process/restart-safe use.
- Seller-side x402 charge integration is still placeholder pending Treasury pattern access/approval.

Next:
- Persist quotes in Postgres.
- Add inbound x402 seller adapter.
- Add gateway tests for get_quote/execute/get_status/get_result against local Postgres.

Questions for humans:
- Should F2 own a `firm_quotes` table migration, or should F3 own all Firm DB schema?

## 2026-07-18

Done:
- Added firm_quotes migration and gateway table bootstrap.
- Changed gateway get_quote to persist quotes in Postgres instead of memory.
- Changed gateway execute to look up non-expired quotes from Postgres before inserting paid firm_jobs.
- Added gateway pricing unit tests.
- Verified:
  - apps/firm-gateway TypeScript build
  - apps/firm-gateway vitest

Blocked:
- Inbound x402 seller charging remains pending Treasury pattern access/approval.
- Gateway execute currently assumes paid request after caller/payment middleware; seller adapter must wrap it before production.

Next:
- Add gateway integration tests against live Postgres for get_quote -> execute -> worker -> get_result.
- Add seller-side x402 middleware.

Questions for humans:
- Is it acceptable for gateway to bootstrap firm_quotes itself at startup, or should all DB setup be migration-only?

## 2026-07-18

Done:
- Gateway-worker live eval validates the gateway DB insert/read path against F3 worker.
- Gateway quote persistence held up through service-level test.

Blocked:
- Inbound seller x402 remains the major F2 blocker before production.

Next:
- Add seller adapter and then make execute unreachable without successful payment.

Questions for humans:
- Should unpaid execute attempts be visible in logs/status, or rejected before any DB write?

## 2026-07-18

Done:
- Gateway DB quote persistence and paid job insertion were verified through live Postgres service evals.
- Gateway get_status/get_result were verified against worker-produced state and provenance.
- Gateway-url demo successfully exercised get_quote -> execute -> worker -> get_result.
- tools/vendor-index remains non-fabricating and requires MARKETPLACE_SCAN_JSON input for real generation.
- Verified:
  - apps/firm-gateway TypeScript build
  - apps/firm-gateway vitest
  - gateway-worker and gateway-procurer live evals through local Postgres

Blocked:
- Inbound seller-side x402 charging remains blocked on Poulav/Treasury pattern approval. execute currently assumes an upstream paid request boundary.
- Live vendor index generation remains blocked on the real marketplace scanner/export.
- express_run remains a TODO_UNVERIFIED_EXPRESS_VENDOR_POOL placeholder until the Express vendor type is verified.

Next:
- Poulav integrates approved seller-side x402 middleware and makes execute unreachable without payment.
- Poulav provides the live marketplace scan JSON/source for vendor-index generation.

Questions for humans:
- Should the first production pricing mode be QUOTED_AMOUNT or TIERS for the July 22 listing?
