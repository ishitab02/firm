## 2026-07-18

Done:
- Read local AGENTS.md, CLAUDE.md, README.md, docs/status/INTERFACES.md, docs/status/PLAN.md, and docs/status/PROMPTS.md.
- Confirmed F4 ownership: packages/mocks, tests, and tools/demo only.
- Noted repository path mismatch: AGENTS/CLAUDE refer to apps/firm/README.md and docs/firm/*, but current repo has README.md and docs/status/*.

Blocked:
- apps/firm/README.md, docs/firm/INTERFACES.md, and docs/firm/PLAN.md do not exist at the binding paths.
- The repo currently has no apps, packages, tests, or tools directories checked in.

Next:
- Start F4 by creating packages/mocks fixture servers first, then tests golden eval scaffolding, then tools/demo scenario runner.
- Keep fixtures aligned to the current INTERFACES contract and make schema/path objections in status instead of patching contracts.

Questions for humans:
- Should the current docs/status/INTERFACES.md and docs/status/PLAN.md be moved/copied to docs/firm/ so every agent can follow the binding prompt literally?

## 2026-07-18

Done:
- Migrated docs/status/INTERFACES.md to docs/firm/INTERFACES.md.
- Migrated docs/status/PLAN.md to docs/firm/PLAN.md.
- Kept docs/status/ for per-agent session logs and kickoff/support notes.

Blocked:
- apps/firm/README.md still does not exist at the binding path; current product README is README.md at repo root.

Next:
- If approved, create apps/firm/README.md from the root README or as a focused worker README so future F3/F4 sessions can satisfy the required read path.
- Then bootstrap F4-owned directories: packages/mocks, tests, and tools/demo.

Questions for humans:
- Should apps/firm/README.md be a copy of the root product README, or a focused README for the Python worker app?

## 2026-07-18

Done:
- Added packages/mocks with dependency-free mock vendor fixtures:
  - vendor_good: reliable schema-valid fresh deliverables.
  - vendor_flaky: deterministic schema + freshness validation failure.
  - vendor_dead: timeout behavior.
- Added mock x402 challenge helpers for both standard and legacy challenge shapes.
- Added a mock vendor HTTP/MCP-style server at packages/mocks/src/server.js.
- Added tests/firm-evals scaffold covering the five golden evals from INTERFACES section 7 against the fixture flow.
- Added tools/demo scenario runner for the flagship simulated spine: quote, trust rejection, hire, firing, re-hire, fixed-price delivery, provenance receipt with absorbed margin.
- Verified:
  - node --test packages/mocks/test/*.test.js
  - node --test tests/firm-evals/*.test.js
  - node tools/demo/scenario.js

Blocked:
- apps/firm/README.md still does not exist. F4 did not create it because AGENTS.md says F4 must never edit apps/* except reading.
- Golden evals currently exercise the fixture flow directly; they need to be wired to the real F3 worker once apps/firm exists.

Next:
- Add a worker adapter in tests/firm-evals once F3 exposes the worker command or HTTP surface.
- Add integration tests that start the mock vendor servers as child processes and call them through the procurer mock/real API once F1 is ready.
- Keep demo live-mode gated until F2 emits data/vendor-index.json and humans approve live runs.

Questions for humans:
- Which F3 worker invocation should the golden eval harness target first: HTTP endpoint, CLI command, or direct pytest fixture?

## 2026-07-18

Done:
- Added tests/firm-evals/worker-live.test.js wiring golden live smoke checks to the real F3 worker CLI.
- Live evals cover delivered fallback path and candidates-exhausted refund path when DATABASE_URL is set.
- Verified live eval with local Postgres:
  - DATABASE_URL=postgresql://firm:firm@127.0.0.1:5432/firm node --test tests/firm-evals/worker-live.test.js

Blocked:
- Full golden evals still need gateway/procurer real-service integration beyond worker smoke commands.

Next:
- Replace smoke-command assertions with gateway -> worker -> result end-to-end checks once apps/firm-gateway and packages/procurer are running together.

Questions for humans:
- Should the golden eval command require local Postgres by default after G2, or keep live evals opt-in through DATABASE_URL?

## 2026-07-18

Done:
- Re-ran live worker evals with DATABASE_URL against local Postgres after F3 refund and queue changes.
- Verified delivered fallback and refund exhaustion paths both pass.

Blocked:
- Gateway/procurer end-to-end evals require running those services together; current live evals target F3 worker CLI.

Next:
- Add service-level eval that starts gateway and procurer, inserts through execute, runs worker, then reads get_result.

Questions for humans:
- Should G2 require service-level evals, or is worker-level live eval acceptable until gateway charging lands?

## 2026-07-18

Done:
- Added gateway-worker live eval that starts local gateway, calls get_quote, execute, runs F3 worker for that task, then reads get_status/get_result via gateway.
- Updated demo runner with --gateway-url mode for local service simulation.
- Added tools/demo start:gateway script.
- Verified gateway-worker live eval against local Postgres.

Blocked:
- Service-level eval still uses F3 DemoProcurer, not Poulav's real procurer/x402 path.

Next:
- Once procurer is running, extend service eval to include gateway -> worker -> HttpProcurer -> mock vendor.

Questions for humans:
- Should the screen-recording demo use fixture mode first, or gateway-url local simulation mode?

## 2026-07-18

Done:
- Added gateway-procurer live eval covering gateway -> worker -> HttpProcurer -> mock vendor with local Postgres.
- Added procurer live eval covering idempotency and cap breach behavior.
- Updated firm eval live script to run DB-backed service tests sequentially to avoid port/service contention.
- Polished tools/demo gateway-url mode for screen recording:
  - quote
  - execute
  - worker run
  - status timeline
  - result/provenance receipt
- Verified gateway-url demo against local Postgres and gateway service; output shows flaky vendor fired, good vendor accepted, and retained margin receipt.
- Verified:
  - node --test --test-concurrency=1 tests/firm-evals/worker-live.test.js tests/firm-evals/gateway-worker-live.test.js tests/firm-evals/procurer-live.test.js tests/firm-evals/gateway-procurer-live.test.js
  - DATABASE_URL=postgresql://firm:firm@127.0.0.1:5432/firm node tools/demo/scenario.js --gateway-url=http://127.0.0.1:8790

Blocked:
- Live-mode demo against the real vendor pool is blocked until F2 emits a real marketplace-derived vendor index and humans approve live operations.
- Real x402 payment evidence is blocked until Poulav/humans provide the approved seller/buyer integration path.

Next:
- Keep fixture mode and gateway-url local simulation ready for screen recording.
- Re-run full live evals after Poulav lands inbound x402 and the real vendor index.

Questions for humans:
- Which exact July 22 listing identity/Agent ID should be used in the final submission evidence?
